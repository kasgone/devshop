{{ 'section-hero.css' | asset_url | stylesheet_tag }}

<div class="header-container">
    <h1>
        <img src="{{ 'top-badges.svg' | asset_url }}" />
        <span>e-shopcreator we create your shopify websites</span>
    </h1>
</div>

{% schema %}
{
    "name": "Hero",
    "presets": [
        {
            "name":"Hero"
        }
    ]
}
{% endschema %}